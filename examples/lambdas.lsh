fnc main() : void {
    add: fnc(int, int) : int = fnc(a int, b int) : int {
        return a + b;
    };

    sub: fnc(int, int) : int = fnc(a int, b int) : int {
        return a - b;
    };

    show(add(1, 2));

    show(sub(2, 1));
}
